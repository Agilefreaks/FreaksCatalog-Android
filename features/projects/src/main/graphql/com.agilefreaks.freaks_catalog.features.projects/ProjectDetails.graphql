query ProjectDetails($projectId: ID!) {
  project(id: $projectId){
    id
    name
    description
    logoUrl{
      uri
    }
    freaks{
      firstName
    }
    technologies{
      name
    }
  }
}